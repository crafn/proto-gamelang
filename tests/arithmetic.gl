let func := fn (a: int, b: int = 10, c: int = 20)
{
};

let main := fn () -> int
{
	func(1, .b= 1);
	/// @todo Prefix operators
	var i : int = 1 * (2 + 3)/2;
	var a : int = 10*i + (i + i)*(2 + 3);
	var b : int = a + (i*a*i - 1 + 1 - 2*(5 + 5 + 5));
	return 0;
};
